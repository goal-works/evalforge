import { PageHeader } from "@/components/ui";

export default function DocumentationPage() {
  return <div className="page prose"><PageHeader eyebrow="Documentation" title="How EvalForge evaluates" description="A compact guide to the V1 execution and scoring contracts."/><section className="panel"><h2>Core workflow</h2><ol><li>Create or select a versioned benchmark.</li><li>Define tasks with inputs and one or more weighted evaluators.</li><li>Select an agent configuration and attempt count.</li><li>Queue the run; a worker records each task attempt and event.</li><li>Inspect output, evaluator reasoning, aggregates, and failures.</li></ol><h2>Evaluator types</h2><p><strong>Exact Match</strong> compares normalized output to a configured expected value. <strong>Contains</strong> checks required text. <strong>JSON Schema</strong> validates JSON type, required properties, and property types. <strong>Deterministic Judge</strong> scores explicit criteria without a paid model.</p><h2>Simplified pass@k</h2><p>The percentage of tasks where at least one attempt succeeded within the first <em>k</em> attempts. Unlike the estimator sometimes used in model research, this metric evaluates the actual attempts persisted by EvalForge.</p><h2>Demo guarantees</h2><p>Mock output uses a stable hash of agent model, task slug, and attempt number. The same seed data and configuration therefore produce the same result on every machine.</p></section></div>;
}
